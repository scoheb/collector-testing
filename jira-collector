import json
import subprocess

data = {}
data['name'] = 'jira-collector'
data['issues'] = [ "SCOTT-1", "SCOTT-2" ]
json_data = json.dumps(data)

subprocess.run(["ls", "-lFRa", "/tekton/creds-secrets"])
subprocess.run(["ls", "-lFRa", "/tekton/home"])

print(json_data)
